# Completion audit — September 12, 2026

This audit supersedes earlier blanket “completed” labels in the research ledger. The full checklist and all twelve phase plans remain in scope. The current baseline passes 240 backend tests (1 opt-in live test skipped) and 36 frontend tests; this does not establish the external or end-to-end gates below.

| Requirement / phase | Evidence gap or defect under repair |
|---|---|
| R1 / 1 | Papers re-verified (ReAct: interleaved reasoning+acting grounded in observations; τ-bench: final-state + pass^k consistency). Tool regression coverage passes this pass (wake-word routing, malformed-argument contract validation, staging/confirm/cancel/barge-in, noncritical states preserved, provider failure visibility, no unsupported success claims). Remaining: rerun the actual provider (AssemblyAI LLM gateway) approval sequence after integration and record the final browser workflow. |
| R2, R6 / 2 | Paper review recorded for this pass (Moshi 160/200 ms full-duplex, Inner Monologue text-prefix streams, 230 ms human response baseline; Amershi G1/G4/G8/G9/G10/G12/G13/G15/G17/G18). Offline/synthetic fidelity verified: speech sanitization preserves technical identifiers, per-reply caption deltas with stale-reply filtering and single final turn, blackbox PCM/WAV recording validity (24 kHz PCM16, source tracks, markers). Physical microphone/speaker rehearsal, reviewed recordings, and timed first-audio/interruption evidence are still missing. |
| R3 / 3 | WAL journal stops on corruption; transactional per-session checkpoints restore isolated browser sessions across a real process exit (subprocess restart test passes), evidence/audio/audit-chain/receipts surviving, expired approvals and interrupted live mutations handled ("outcome unknown", never replayed), logout/TTL purging recordings and fail-closed on checksum mismatch. 11 focused tests pass. Remaining: confirm identical behavior in the deployed runtime and record a process-restart demo. |
| R4 / 4 | RBAC repair completed offline: no published/built-in credentials, rotation invalidates cookies and cached permissions, revocation persists across registries and closes a live WebSocket, role change invalidates cached commander access, unknown/anonymous identities denied, individually attributed audit chain, live-mode identity without shared secret. 31 focused auth cases plus 14 audit regressions pass (36 total, incl. regressions). Remaining: deployed-runtime revocation flow and recorded browser workflow. |
| R5 / 5 | Golden signals with `measured_at` per service, real host network/disk/process metrics via psutil, quantitative remediation receipt deltas with `verified_improvement`, and `verify_recovery` SLO gating all verified offline; 11 focused tests pass. Remaining: evidence against configured live Docker/Kubernetes targets with outcome receipts. |
| R7 / 6 | Benchmark harness completed this pass: 61 turns (≥50 required), reproducible offline run with provisioned operators and pinned mock LLM, per-turn pass accounting (no double count). Recorded run: 100% pass, 100% safety. Live pilot protocol run by a human team and MTTD/MTTR measurement remain pending (a protocol is not a completed pilot). |
| R8, R9 / 7 | Deployment artifacts verified offline: multi-stage Dockerfile with healthcheck, compose with durable volume + env wiring, render.yaml/fly.toml consistent, `dev.sh` executable, production build + 36 frontend tests pass, submission checklist refreshed. Remaining: actual image build/deployment to a reachable HTTPS URL, recorded final demo video, current presentation review, and real submission links. |
| R10 / 8 | Web/docs/media tools verified offline: search with curated fallback + source URLs, PDF/DOCX inspection with page citations, valid PDF/DOCX report export, media transcription with labeled simulation fallback, orchestrator routing + role permission gating; 6 focused tests pass. Remaining: live AssemblyAI Transcriber / real HTTP search against actual files with attribution evidence. |
| R11 / 9 | Reflexion episodic buffer (Ω=3), memory stream triad retrieval, and verbal self-critique generation verified offline; reflection memory strictly records operational context and is guarded against auto-dispatching destructive actions without approval; 17 focused tests pass. Remaining: verification in multi-operator live incident drill. |
| R12 / 10 | Causal topology root-cause localization (MicroHECL-style anomaly flow), DéjàVu cosine signature matching at τ=0.70 with proven playbooks, per-epoch barge-in interrupt events, and MTTR computed from the timeline (not hardcoded catalog values) all verified offline; reflection never auto-dispatches destructive actions; 17 focused tests pass. Remaining: matching against real measured incident history and live/recorded barge-in evidence. |
| R13 / 11 | ToT world-model rollouts, BFS with pruning (τ=0.30), Pareto-optimal multi-step plans with predicted metric gains, and a safety filter that removes unapproved destructive steps and marks destructive actions `needs_approval` all verified offline; 18 Phase 11/12 tests pass. Remaining: live plan-and-approve session validation of candidate actions, simulated transitions and safety constraints. |
| R14 / 12 | Paper review for the revised plan is recorded (Leviathan ICML 2023, Bhendawade 2024, Yusuf Interspeech 2024). RBAC mediation repaired here: Phase 8–12 read-only tools added to `READ_ACTIONS`, prefetch gated on allowlist + `is_action_permitted`, cache-hit path revalidates permission. Freshness (TTL) is enforced; the actual streaming-partial latency and <2 ms claim still need a timed live measurement.
| R15 / dogfood | Runtime dogfood pass executed this session: real server exercised over HTTP/WebSocket (29/29 checks: health, auth rejection, session open/close/revocation, cluster/topology/runbooks/audit/security reads, runbook start/advance/abort, postmortem, WS ping/barge-in, malformed-message handling). Frontend build, 36 vitest tests, and real-backend Chrome smoke pass. Fixed a 320px horizontal overflow (`.runbook-session-heading` wraps now) and guarded it in the browser smoke (active runbook + 320px viewport). `validate_providers.py` / `engineer_live_validation.py` now require an explicit `--live` flag (they previously ran live provider calls when `--help` was requested; one AssemblyAI call hit an HTTP 429 and fell back locally). Full suite re-verified this pass: **240 backend tests passed, 1 opt-in live test skipped; 36 frontend tests** (supersedes the earlier "224 backend" figure, which predated the Poolside/audio commits). Remaining external gates unchanged: deployed HTTPS, mic rehearsal, live pilot.

## Authorization repair: papers reviewed before the revised plan

- [Saltzer and Schroeder (1975), §I.A.3](https://web.mit.edu/Saltzer/www/publications/protection/Basic.html), read directly on the authors’ MIT site: default permission must be explicit, and cached authority must change when permission changes. Application: remove public credentials and recheck current credential state at session lookup and tool dispatch, including existing sockets.
- [Sandhu et al. (1996), printed p. 41, base model and sessions](https://www.profsandhu.com/cs5323_s18/RBAC96-1996.pdf), read from the author-hosted scanned page: users, roles, permissions and sessions are distinct; a session belongs to one user. Application: persist explicit operator assignments and preserve the same browser session on authenticated refresh, while switching identities creates a separate session. This is a limited application of the model, not a security certification.

### Revised implementation plan

1. Start with an empty operator directory. Persist operator token hashes and revocations in a transactional local SQLite store; provide a local provisioning command without public default tokens.
2. Rotate credentials atomically, reject duplicate/revoked tokens and invalidate older sessions. Recheck current identity, token and role on protected requests and tool execution.
3. Preserve incident state when an authenticated browser refreshes. Require explicit identity authentication in live mode and when the directory has configured operators.
4. Add regressions for published defaults, rotation, restart revocation, session refresh, role changes, unknown identities and existing connections. Rerun the backend suite and browser flow.

Status: implementation in progress. Deployment, microphone, pilot and final-media gates are still pending; no external result is inferred from local tests.

Authorization validation: 46 focused backend cases passed, including rotation, persistent revocation, role changes, authenticated refresh and a live WebSocket closing after revocation. The production frontend build, 36 frontend tests and real-backend Chrome workflow passed on desktop and mobile. Full-suite validation follows integration.

## Recovery repair: papers reviewed before the revised plan

- [Mohan et al., ARIES (1992), §3 and §13](https://www.cs.cmu.edu/~15849g/readings/mohan92.pdf), original paper read through the CMU-hosted copy. ARIES uses per-page state, redo and compensation records for interrupted transactions. An append-only event list that skips corrupt records does not implement those guarantees. Application: use database transactions for committed session snapshots, retain durable mutation intent, and never replay external infrastructure operations on restart. This application will not claim ARIES compliance.
- [Gao et al., ALCE (EMNLP 2023), §3.3](https://aclanthology.org/2023.emnlp-main.398.pdf), reviewed earlier in this work. Citation attribution depends on preserved supporting material. Application: restore the exact captured observations and receipt data with the incident, not a newly generated evidence baseline. Reference existence alone does not prove a hypothesis.
- Implementation reference: [SQLite WAL documentation, §2–3](https://sqlite.org/wal.html). Use local storage and FULL synchronous commits; let SQLite handle its transaction recovery. Keep the deployment to one application worker because active session services and locks remain in memory.

### Revised implementation plan

1. Persist browser session identity and an explicit, versioned snapshot of incident/service state, evidence, receipts, audit blocks, transcript, runbook progress and blackbox metadata. Store PCM chunks as separate blobs with offsets and source tracks.
2. Restore lazily by the existing session cookie, revalidate current operator credentials and enforce the eight-hour retention window. Never combine records from separate sessions. Expire pending approvals and report interrupted live mutations as uncertain.
3. Commit state at HTTP and WebSocket outcome boundaries; record mutation intent before infrastructure execution and outcome before returning a receipt. Fail visibly on storage errors instead of silently claiming durability.
4. Keep per-session event logs separate and reject a corrupted sequence; remove global startup replay. Verify two browser sessions, exact evidence/audio retention, expired approval, logout and interrupted-write behavior across process restarts.

Status: implementation in progress.
